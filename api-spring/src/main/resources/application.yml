server:
  port: 8080
app:
  jwt:
    secret: "change-me-in-prod"
    issuer: "marketplace-suite"
    expirySeconds: 3600
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui
